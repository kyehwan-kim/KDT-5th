let mathScorestr = prompt("수학 점수를 입력하세요");
let engScorestr = prompt("영어 점수를 입력하세요");

let mathScoreNum = Number(mathScorestr);
let engScoreNum = Number(engScorestr);
let avg = (mathScore + engScore) / 2;

console.log(Boolean(avgScore));
